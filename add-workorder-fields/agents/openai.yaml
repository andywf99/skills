interface:
  display_name: "工单新增字段"
  short_description: "普通工单新增字段时的联动改造、DDL、ES、导出与回刷指引"
  default_prompt: "Use $add-workorder-fields to implement or review dynamic work order fields across entity, DTO/VO, mapper, ES, DDL, export, backfill, and dependency reporting."
